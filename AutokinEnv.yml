name: AutokinEnv
channels:
  - pytorch
  - defaults
  - conda-forge
dependencies:
  - pytorch
  - numpy
  - matplotlib
  - tensorboard
  - roboticstoolbox-python
  - tqdm
  - pytest
  - hypothesis

# Nuevo env
# conda env create -f AutokinEnv.yml
# Actualizar env
# conda env update -f AutokinEnv.yml --prune